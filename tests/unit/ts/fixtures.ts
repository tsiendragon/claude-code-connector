/**
 * fixtures.ts — DEPRECATED inline fixtures.
 *
 * Test fixtures have moved to tests/fixtures/<NNN>-<name>/ folders.
 * Each folder contains:
 *   frames/01.txt, 02.txt, ...  ← terminal captures
 *   expected.json               ← expected function outputs
 *
 * See tests/unit/ts/parser.fixtures.test.ts for the auto-discovery runner.
 */
